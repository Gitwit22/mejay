# Intentionally empty.
# Cloudflare Pages already defaults to SPA rendering when no top-level 404.html exists.
# Routing config (if needed later): see /_routes.json

# SPA fallback (Cloudflare Pages / Netlify-style redirects)
/* /index.html 200
